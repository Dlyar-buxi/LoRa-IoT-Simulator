# LoRa IoT Simulator
# Python 3.12+
# Minimal runtime dependencies. Step 1 (simulator/) runs on the standard library alone.

# ===== Web / API =====
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# ===== MQTT telemetry export =====
paho-mqtt==2.1.0

# NOTE: SQLite experiment persistence uses the standard-library `sqlite3` module —
# no extra dependency is required.
#
# Removed during Sprint 5.4 packaging (unused by the running platform):
#   SQLAlchemy, numpy, pandas, matplotlib, folium
